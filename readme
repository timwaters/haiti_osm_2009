These files were if I recall correctly the imported and merged haiti import roads and existing roads as of Jan 2009. The import dataset came from the CNIGS (Haiti Mapping Agency) via CartOng. 

The osm files also contain a few buildings and tracks which were there before the major import.

OSM License as of 2009: CC-By-SA
